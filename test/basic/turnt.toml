[envs.print]
command = "fdpo < {filename}"
output."rt" = "-"

[envs.run]
command = "fdpo run {args} < {filename}"
output.out = "-"

[envs.ask-run]
command = "fdpo ask-run {args} < {filename}"
output.out = "-"
default = false

[envs.smt]
command = "fdpo smt {args} < {filename}"
output.smt = "-"
